# 004h PostgreSQL 首个计划与14天体验原子激活云端验收

验收日期：2026-08-24（Asia/Shanghai）

## 结论

004h 首个正式计划与14天体验原子激活 RPC 已在腾讯云 PostgreSQL `diet_secretary` 完成部署，并在 CloudBase 版本019通过真实私网数据库回滚沙箱验收。

- 部署前只读检查：`PASS`
- 正式迁移：执行成功
- 部署后函数、权限和原子定义检查：`PASS`
- DMS功能回滚沙箱：5/5项 `PASS`，清理 `PASS`
- CloudBase版本019真实PostgreSQL适配器验证：7/7项 `PASS`
- 最终清理：`PASS`
- 线上业务存储适配器：仍为 `sqlite`

## 已验证能力

1. 首个计划必须从 `draft` 激活，服务必须从 `profile_confirmed` 启动体验。
2. 计划激活、计划转换历史、体验状态和服务转换历史由单一SECURITY DEFINER RPC原子写入。
3. 体验期严格为14×24小时，续费提醒严格安排在第13×24小时。
4. 同一计划和同一体验周期重复调用不会新增转换历史，具备安全幂等语义。
5. 体验开始时间早于计划创建时间时以SQLSTATE `22023` 拒绝，计划保持draft且服务保持profile_confirmed。
6. 用户B无法读取用户A的计划或转换历史。
7. 适配器正确映射计划与UTC时间，并只使用参数化查询。
8. CloudBase验证受显式确认、SQLite保持和RFC1918私网地址护栏保护。
9. 沙箱事务回滚后，两个沙箱用户的用户、服务状态、服务历史、能量计算、计划及计划历史残留均为0。

## 能力清单

- UserStore契约方法：38
- PostgreSQL数据库已就绪：27
- 仍需数据库结构：9
- 仍需契约调整：2
- PostgreSQL生产切换条件：尚未满足

## 验证脚本修复说明

DMS功能沙箱首次使用了非法自定义参数名 `app.004h_verify_plan_a`，在功能断言前中止且没有提交事务。脚本随后改为合法名称 `app.verify_004h_plan_a`，增加负向回归断言，并在新会话重跑；修复后的5项DMS结果全部通过。因此最终结论以修复后结果和CloudBase 019回滚沙箱为准。

## 数据边界

- 证据只保存布尔结果、计数、白名单错误码和沙箱汇总。
- 不保存真实用户ID、计划内容、身体数据、密码、数据库地址或连接串。
- DMS与CloudBase功能验证均使用回滚沙箱。
- 本批未修改 `USER_STORE_ADAPTER`；CloudBase版本019验证时线上业务存储仍为SQLite。

## 完整性校验

在本目录运行：

```sh
shasum -a 256 -c MANIFEST.sha256
```
