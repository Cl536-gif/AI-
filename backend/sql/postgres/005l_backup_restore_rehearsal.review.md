# 005l PostgreSQL 备份恢复、RPO/RTO 与传输安全演练

## 目标

在不切换生产 UserStore、不覆盖源实例的前提下，证明当前腾讯云 PostgreSQL 的物理备份与日志链可以恢复到指定时间点，并验证恢复后的结构、固定恢复边界、RPO/RTO 和 `verify-full` TLS。

## 官方产品边界（2026-08-26复核）

- 腾讯云 PostgreSQL 使用物理全量备份与 WAL 日志备份。
- PITR 可恢复范围依赖日志备份保留；删除日志会破坏时间点恢复连续性。
- 克隆支持按备份集或指定时间点恢复到新实例，源实例访问不受克隆影响。
- 克隆实例默认不继承 SSL，需要在克隆实例上单独开启；开启/关闭 SSL 会重启实例。
- 控制台展示的保留范围因实例版本/形态可能不同，本门禁使用实际观察值，不硬编码控制台产品上限。

参考：

- https://cloud.tencent.com/document/product/409/94889
- https://cloud.tencent.com/document/product/409/68277
- https://cloud.tencent.com/document/product/409/131309
- https://intl.cloud.tencent.com/zh/document/product/409/67850

## 固定顺序

1. 保持正式 `diet-secretary-api` 为 SQLite；不得修改生产服务。
2. 在控制台确认最近成功物理备份、数据/日志保留天数、日志备份开启状态；记录观察值，不记录实例ID、地址或凭据。
3. 在DMS执行只读 preflight，确认源实例SSL已开启、标记不存在并记录三项结构计数；任一条件不满足时状态必须为`BLOCKED`。
4. 执行 seed，记录 `before_marker_at`；至少等待两分钟后执行 cutoff，保存四个UTC时间。
5. 选择 cutoff 输出的 `recommended_recovery_target_at`，以“按时间点克隆”创建隔离恢复实例；记录恢复开始时间。
6. 克隆就绪后单独开启 SSL，下载 CA；仅在临时验证服务中以 Base64 密文环境变量配置 CA，连接模式固定 `verify-full`、池上限1。
7. 用源库地址在本机计算 SHA-256 指纹，只保存摘要；恢复验证器拒绝连接同一地址。
8. 在独立临时服务运行本地云守卫和恢复验证；验证器只读检查前标记存在、后标记不存在、结构计数一致、TLS会话有效、RPO/RTO达标。
9. 先在源库执行精确 cleanup，再执行 preflight 证明标记消失；随后删除临时服务和隔离克隆实例。
10. 只有备份、恢复、TLS、RPO/RTO、源库清理和云资源删除全部有证据时才归档通过。

## 默认验收目标

- 建议初始 RPO 目标：30分钟。
- 建议初始 RTO 目标：120分钟。
- 最近成功全量备份最大年龄：不超过72小时，且声明上限不得超过84小时。
- 数据与日志保留建议不少于30天；日志保留不得短于数据保留。

这些目标是当前小规模项目的候选门槛，不会自动修改云资源；实际数值必须由项目负责人确认并作为环境变量输入。

## 失败关闭

以下任一情况立即停止：日志备份关闭、最近备份过旧、恢复点不在两标记之间、连接仍指向源地址、TLS不是CA校验的`verify-full`、结构计数不同、恢复出现后标记、RPO/RTO超目标、源标记无法精确清理或克隆/临时服务无法删除。

## 不在本轮范围

- 不执行原实例回档。
- 不下载或导出真实数据库内容。
- 不将恢复实例接入正式流量。
- 不授权全量 PostgreSQL 切换。
- 不把地址、实例ID、证书、密码、token、用户标识或数据正文写入Git/Obsidian。
