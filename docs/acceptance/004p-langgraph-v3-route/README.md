# 004p LangGraph V3 正式路由云端验收

验收日期：2026-08-25（Asia/Shanghai）

## 结论

004p LangGraph V3 正式路由已在默认 SQLite 配置下完成本地回归、CloudBase 包内测试和真实 HTTP 行为验证。

- 异步身份解析与持久化顺序：`PASS`
- 非法设备身份在图执行前拒绝：`PASS`
- 直接问题优先回答：`PASS`
- 回访用户上下文恢复：`PASS`
- 计划修订对话与生成关键场景：`PASS`
- 空档案事实一致性：CloudBase 036 `PASS`
- 服务健康检查：HTTP 200 `PASS`
- 线上业务存储适配器：仍为 `sqlite`

## 已验证能力

1. 请求先完成匿名身份解析，再读取上下文、执行LangGraph和持久化副作用。
2. 非法`deviceId`在图执行前返回HTTP 400。
3. 明确问题先得到回答，随后恢复未完成的资料收集流程。
4. 已有档案的新会话进入回访流程；真正的新用户仍进入首次六项采集。
5. 计划修订能够收集变化、准备计算输入并生成结构化新版草稿命令。
6. 身高、体重、年龄、经期日期和长文本输出等浏览器回归保持通过。
7. 空档案用户的模型回答不得把食堂、外卖、固定套餐、口味或运动当成既有事实。
8. 模型连续三次仍输出无依据场景时，系统使用确定性通用餐盘建议降级。

## 迭代说明

- CloudBase 033首次真实请求暴露“老底子”式伪记忆。
- CloudBase 034仍把“食堂自己打饭”写成既定事实。
- CloudBase 035又以“食堂常见搭配”绕过固定措辞检测。
- 最终修复改为场景级事实约束：只有用户本轮明确提供的场景或明确条件句才可使用；CloudBase 036以全新匿名身份复验通过。

## 发布边界

- 本批不修改数据库结构。
- 线上`USER_STORE_ADAPTER`未切换为`tencent-postgres`。
- 验收材料不保存设备ID、会话ID、实例ID、API Key、应用ID、连接串或真实用户信息。
- 下一阶段需单独制定单实例、可回滚的PostgreSQL灰度方案。

## 完整性校验

在本目录运行：

```sh
shasum -a 256 -c MANIFEST.sha256
```
