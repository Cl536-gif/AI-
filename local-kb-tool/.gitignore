node_modules/
data/
.env
npm-debug.log*

# 知识库原文可能是未公开的业务资料，默认不提交，只在本地保留
# 用 **/*.docx 而不是 *.docx，是为了同时覆盖 --kb <名字> 用的 kb-docs/<名字>/ 子目录
kb-docs/**/*.docx

# 检索效果测试报告，每次跑出来的内容都不一样，属于运行输出，不提交
eval-reports/*.md
!eval-reports/.gitkeep
