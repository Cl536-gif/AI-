# 004l PostgreSQL 本人数据快照云端验收

验收日期：2026-08-25（Asia/Shanghai）

## 结论

004l 本人数据快照已在 CloudBase 版本025通过真实私网 PostgreSQL 回滚沙箱验收。本批不新增数据库对象，仅顺序组合已验收的8类本人读取。

- 本地快照与映射验证：`PASS`
- 能力清单：35/38项 `database_ready`
- CloudBase版本025真实PostgreSQL适配器验证：8/8项 `PASS`
- 最终清理：`PASS`
- 线上业务存储适配器：仍为 `sqlite`

## 已验证能力

1. 快照顺序组合本人档案、档案修订、服务状态、建议历史、事件、能量计算、计划和服务转换历史。
2. 8类数据的字段映射、JSON内容与UTC时间在云端实例中一致。
3. 用户B不会在用户A的快照中出现，空用户快照保持安全默认值。
4. 重复读取不会修改任何数据。
5. 读取按顺序执行，本地测试的最大并发事务数为1，适配CloudBase单连接池验证。
6. CloudBase验证受显式确认、SQLite保持、RFC1918私网地址、测试池上限1和连接超时护栏保护。
7. 所有测试数据位于事务回滚沙箱，验证后无残留。
8. `listUserSummaries`因缺少管理员身份、授权与审计上下文被保留为契约调整项，未向普通`diet_app`授予跨用户读取。

## 能力清单

- UserStore契约方法：38
- PostgreSQL数据库已就绪：35
- 仍需数据库结构：0
- 仍需契约调整：3
- PostgreSQL生产切换条件：尚未满足

## 失败夹具的处理

- 版本023首次验证因测试`source`超过既有长度限制而返回`22001`。
- 版本024验证因缩短后的测试`source`仍不在既有白名单而返回`23514`。
- 两次失败均在事务回滚沙箱内，未提交业务数据。最终夹具使用合法白名单值`system`，版本025通过。

## 数据边界

- 证据只保存布尔结果、计数、错误代码和沙箱汇总。
- 不保存真实用户ID、档案内容、身体数据、密码、数据库地址或连接串。
- 本批未修改`USER_STORE_ADAPTER`；CloudBase版本025验证时线上业务存储仍为SQLite。

## 完整性校验

在本目录运行：

```sh
shasum -a 256 -c MANIFEST.sha256
```
