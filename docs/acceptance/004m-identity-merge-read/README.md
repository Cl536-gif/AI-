# 004m PostgreSQL 身份合并读取云端验收

验收日期：2026-08-25（Asia/Shanghai）

## 结论

004m 身份合并读取与管理端口拆分已在 CloudBase 版本027通过真实私网 PostgreSQL 回滚沙箱验收。本批不新增数据库对象，复用既有本人合并结果与审核读取 RPC。

- 本地生产 UserStore 契约：37/37项 `database_ready`
- CloudBase版本027真实PostgreSQL适配器验证：7/7项 `PASS`
- 最终清理：`PASS`
- 线上业务存储适配器：仍为 `sqlite`

## 已验证能力

1. 合并结果读取必须显式传入已认证目标账号与来源账号。
2. 合并审核读取必须显式传入已认证目标账号与合并ID。
3. 合并结果、冲突、审核和事件字段可从既有PostgreSQL RPC正确映射。
4. 其他账号不能读取目标账号的合并结果或审核记录。
5. `listUserSummaries`不属于生产UserStore契约，仅保留为非生产管理调试能力。
6. CloudBase验证受显式确认、SQLite保持、RFC1918私网地址、测试池上限1和连接超时护栏保护。
7. 所有夹具位于事务回滚沙箱，验证后无残留。

## 能力清单

- 生产UserStore契约方法：37
- PostgreSQL数据库已就绪：37
- 仍需数据库结构：0
- 仍需契约调整：0
- 独立管理方法：1（不进入生产适配器）
- PostgreSQL生产切换条件：尚未满足；仍需异步调用审计、全契约回归和可回滚灰度验证

## 数据边界

- 证据只保存布尔结果、计数、批次名、进程号和沙箱汇总。
- 不保存真实用户ID、合并内容、密码、数据库地址或连接串。
- 本批未修改`USER_STORE_ADAPTER`；CloudBase版本027验证时线上业务存储仍为SQLite。

## 完整性校验

在本目录运行：

```sh
shasum -a 256 -c MANIFEST.sha256
```
