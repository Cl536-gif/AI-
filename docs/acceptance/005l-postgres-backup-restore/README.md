# 005l PostgreSQL备份恢复与传输安全验收

验收日期：2026-08-26（Asia/Shanghai）

## 结论

005l 已完成源库备份/WAL盘点、固定恢复边界、隔离PITR克隆、`verify-full` TLS、结构一致性、RPO/RTO、源库精确清理和临时资源删除闭环。

- 物理备份与日志备份：`PASS`
- 数据/日志保留：`7 / 7`天
- 最近成功物理备份年龄：不超过`72`小时
- 源库SSL与CA链：`PASS`
- 固定恢复边界：仅`before`存在、`after`被排除
- schema结构：`23`表、`40`函数、`167`约束
- TLS会话：`TLSv1.3`，`verify-full`
- 实际RPO：`19`分钟，目标`30`分钟
- 实际RTO：`43`分钟，目标`120`分钟
- 源实例未被恢复验证器改写：`PASS`
- 源库固定canary精确清理：`PASS`
- 清理后preflight：`PASS`
- 临时验证服务与按量恢复克隆删除：`PASS`

## 已验证能力

1. 源实例可依据物理备份和连续日志链恢复到隔离的新实例，不覆盖源实例。
2. 恢复点位于固定前后标记之间，恢复实例仅包含前标记，不包含后标记。
3. 恢复后的表、函数和约束数量与源库只读基线一致。
4. 应用通过CA校验及主机绑定的`verify-full`建立TLSv1.3连接。
5. 实际RPO与RTO均满足本轮30/120分钟目标。
6. 表盘点改用`pg_class`真实schema对象口径，不受应用角色授权范围过滤。
7. 源库测试表仅在固定两条marker完全匹配时删除，清理后再次只读验证结构与SSL状态。
8. 临时CloudBase服务和按量PITR克隆均已删除，不再持续计费。

## 失败轮次与纠正

- 首轮短窗口克隆同时包含前后标记，按失败关闭处理并删除。
- 第二轮推荐中点距故障观察约79分钟，超过30分钟RPO目标，未放宽指标并删除该克隆。
- 纠正版选择距故障观察19分钟的真实恢复点，最终边界、TLS、结构、RPO和RTO全部通过。
- 恢复校验曾因`information_schema.tables`按角色过滤显示16张表；未降低23张表基线，而是统一为系统目录真实schema口径后复验通过。

## 发布边界

- 本验收证明当前腾讯云PostgreSQL备份链、隔离PITR和传输安全可用。
- 本验收不等于005m最终Go/No-Go，不自动授权全量PostgreSQL切换。
- 本验收没有恢复或输出真实用户数据，也没有对正式源实例执行回档。
- 验收档案不保存地址、实例ID、域名、账号、密码、连接串、证书正文、Pod名称、用户标识或数据正文。

## 完整性校验

在本目录运行：

```sh
shasum -a 256 -c MANIFEST.sha256
```
