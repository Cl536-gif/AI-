# 004f PostgreSQL 能量计算审计云端验收

验收日期：2026-08-24（Asia/Shanghai）

## 结论

004f 能量计算审计表与受控写入RPC已在腾讯云 PostgreSQL `diet_secretary` 完成部署，并在 CloudBase 版本017通过真实私网数据库回滚沙箱验收。

- 部署前只读检查：`PASS`
- 部署后对象、约束、RLS与权限检查：`PASS`
- DMC功能回滚沙箱：全部 `PASS`，清理 `PASS`
- CloudBase版本017真实PostgreSQL适配器验证：7/7项 `PASS`
- 最终清理：`PASS`
- 线上业务存储适配器：仍为 `sqlite`

## 已验证能力

1. 每次计算生成不同ID并以不可变记录追加保存。
2. 历史按 `created_at` 和计算ID倒序读取，最新记录优先。
3. 公式ID、公式版本、标准化输入、假设、输出、来源和创建时间完整映射。
4. 应用提供的带偏移时间统一返回UTC ISO 8601格式。
5. 适配器只发送白名单字段，额外领域字段不会进入数据库RPC。
6. 未知字段、缺失输出和错误JSON类型以SQLSTATE `22023` 拒绝，且不新增记录。
7. 用户B无法读取用户A的能量计算历史。
8. `diet_app` 只能读取自己的计算记录；追加写入必须经过SECURITY DEFINER RPC。
9. 沙箱事务回滚后用户和计算记录残留均为0。

## 执行说明

DMC页面一度重复显示上一批的旧错误。随后再次执行迁移时返回 `energy_calculations already exists`，表明首次执行已经创建对象。没有继续重跑或删除表，而是执行只读后置检查；该检查确认表、9列、6个检查约束、RLS策略、索引、RPC所有权、执行权限和最小表权限全部正确，且记录数为0。因此以只读最终状态和功能沙箱结果作为验收依据。

## 数据边界

- 证据只保存布尔结果、计数、白名单错误码和沙箱汇总。
- 不保存真实用户ID、身体数据、计算输入、密码、数据库地址或连接串。
- DMC与CloudBase功能验证均使用回滚沙箱。
- 本批未修改 `USER_STORE_ADAPTER`；CloudBase版本017验证时线上业务存储仍为SQLite。

## 完整性校验

在本目录运行：

```sh
shasum -a 256 -c MANIFEST.sha256
```
