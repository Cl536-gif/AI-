# 专属饮食秘书

需求文档：`专属饮食秘书_最简后端需求文档.md`（由需求方提供，未收录进本仓库）。

## 当前进度

已完成需求文档「二、核心功能清单」中的：

- [x] 2. 后端中间层服务 —— 见 `backend/`
- [x] 3. 密钥安全存储 —— 密钥通过 `.env` 管理，不提交到仓库

尚未开始（按需求方要求，后续再做）：

- [ ] 1. 对话页面（前端）
- [ ] 4. 简易用户身份 + "上次活跃时间"记录
- [ ] 5. 回复内容安全检查

## 目录

- `backend/` — Node.js + Express 后端中间层，转发对话请求到阿里云百炼。详见 `backend/README.md`。
